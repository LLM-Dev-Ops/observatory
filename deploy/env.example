# LLM Observatory - Environment Configuration
# Copy to .env and configure for your environment
# NEVER commit actual secrets to version control

# ============================================================================
# SERVICE IDENTITY
# ============================================================================
SERVICE_NAME=llm-observatory
SERVICE_VERSION=1.0.0
PLATFORM_ENV=dev  # dev | staging | prod

# ============================================================================
# RUVECTOR SERVICE (Memory/Persistence Layer)
# ============================================================================
# REQUIRED: URL to the ruvector-service instance
RUVECTOR_SERVICE_URL=https://ruvector-service.internal

# REQUIRED: API key for ruvector-service authentication
# In production, use Google Cloud Secret Manager instead
RUVECTOR_API_KEY=

# ============================================================================
# TELEMETRY (Self-Observation)
# ============================================================================
# Optional: Endpoint for Observatory's own telemetry
TELEMETRY_ENDPOINT=

# Enable/disable self-observation (default: true)
SELF_OBSERVATION_ENABLED=true

# ============================================================================
# OPERATIONAL SETTINGS
# ============================================================================
# Log level: debug | info | warn | error
LOG_LEVEL=info

# Maximum batch size for telemetry ingestion
MAX_BATCH_SIZE=100

# Request timeout in milliseconds
REQUEST_TIMEOUT_MS=30000

# Cloud Run port (do not change)
PORT=8080

# ============================================================================
# NOTES
# ============================================================================
# - LLM-Observatory does NOT connect directly to Google SQL
# - All persistence occurs via ruvector-service
# - No agent hardcodes service names or URLs
# - All secrets must be provided via environment or Secret Manager
